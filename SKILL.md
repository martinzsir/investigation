---
name: sunzi-investigation
description: >
  侦查线索的结构化推演内核。把举报、银行流水、通话记录、招投标档案、轨迹等数据
  展开为可溯源的候选线索，输出「假设—证据—程序」对照、五间交叉等级、
  优先级排序与处置状态看板。
  Use when: 需要盘案件要素、扫描资金/通讯/轨迹异常、识别第三方过桥结构、
  判断多条线索是否达到交叉升格门槛、跟踪线索处置进度。
  Do NOT use for: 给案件定性、决定立案、替代法定程序——定性权只属于人（正兵）。
  不要让它读取数据后直接下结论；它输出的是「候选 + 溯源 + 待核实标记」。
---

# 孙武侦查官 · Skill（DuckDB 单机版）

> 侦查逻辑（庙算→知己知彼→虚实→奇正→用间→全胜）与数据量、与模型**完全解耦**。
> 本技能是**确定性计算内核**，跑完出结构化产物；LLM 只负责编排与表达，不碰计算。

## 定位
侦查员的"决策副驾"，只做三件事：
1. 把案件要素展开成可复查的推演（庙算）
2. 把全量数据里的反常标成候选突破口（虚实/用间）
3. 把"假设—证据—程序"对齐，把定性权交回人（奇正/全胜）

## 技术栈（单机 / 笔记本 / 机架单机 通用）
```
L1 热   Redis（可选）      特征 / 五间命中 / 覆盖度        ms
L2 温   DuckDB 单文件      预聚合(主体×月) + 物化视图      s   ← 替代 StarRocks
L3 冷   Parquet 分区       原始流水 / 通话 / 中标          min
L4 图   LadybugDB（可选）  关系多跳 / 过桥 / 串并          s
L5 增量 Python 脚本        cron 跑新增分区 → 更新 DuckDB   ← 替代 Flink
```

## 核心决策顺序（不可颠倒）
庙算 → 知己知彼 → 虚实 → 奇正 → 用间 → 全胜

## 五条不可让渡红线
1. AI 不出定性结论、不替代法定程序
2. 每条推断必须挂 `source_rows` 溯源（回到 L3 Parquet 原始行）
3. 严格区分【数据事实】【推断】【待核实】三栏
4. "知己"栏必须填证据缺口与授权边界，不准只画对象画像
5. 单源情报不算数，至少 2 类"间"独立指向才升格

## 五子技能
- `skill: miaosuan` 庙算沙盘：假设 ≤5 条，自动证伪条件
- `skill: zhi_ji_zhi_bi` 双向画像机：知己强制非空
- `skill: xu_shi` 虚实扫描：只标反常，不给定性（DuckDB 扫描 Parquet）
- `skill: qi_zheng` 奇正分工器：奇兵拓线 / 正兵固证 双列
- `skill: yong_jian` 五间交叉器：单源=观察 / 双源=线索 / 三源=可立案依据候选

---

## DuckDB 替代 StarRocks 的关键设计
- L2 是 **单个 `investigation.duckdb` 文件**，含 `agg_*` 预聚合表 + 物化视图
- 冷层 Parquet 通过 `read_parquet(...)` 直接查询，零 ETL
- 增量：`python -m scripts.incremental --quarter 2024-Q4` 只扫新分区 → 更新 L2
- 所有 SQL 用 ANSI 标准（窗口/聚合/CTE），保证可移植

> ⚠️ **更正**：早期版本文档曾写"图库边表可由 `duckdb_attach` 直接从 DuckDB 物化生成"。
> 该说法**未经验证且当前不可行**——LadybugDB 的 `duckdb` 扩展需运行时下载，
> 受限网络下拿不到（实测 403 策略拒绝）。
> **实际可行路径是 CSV 中转**：DuckDB 导出 CSV → `COPY` 进 LadybugDB → Cypher 多跳。
> 见 `scripts/verify_ladybug.py`（能力验证）与 `scripts/q2_overpass_cypher.py`（实测）。

---

## LLM 与内核的边界（接入 agent 时必读）

### 内核负责「算」，LLM 负责「编排与表达」

| 环节 | 谁做 | 理由 |
|---|---|---|
| 扫描异常、交叉升格、多跳查图 | **内核** | 确定性、可复现、可审计 |
| 读 Parquet、写业务 SQL | **内核**（禁止 LLM 直接做） | 2000 亿行进不了上下文；LLM 生成的 SQL 无法复现审计 |
| 组织流程、追问、排序展示 | LLM | 需要语言理解 |
| 把结果写成人读的待办清单 | LLM | 表达层，不改数据 |
| 从举报信/笔录抽取假设雏形 | **LLM**（唯一介入计算的环节） | 非结构化文本是 LLM 强项；且假设自带证伪条件，猜错会被数据降级 |

### 三条硬约束（对 LLM）
1. **不要自己写业务 SQL** —— 一律走工具（MCP / 函数），否则结果不可复现
2. **不要把原始明细搬进上下文** —— 只要 `source_rows` 的溯源 ID 和聚合结果
3. **不要下定性结论、不要置"已立案"** —— 内核在工具层强制拦截，不靠 prompt 自觉

### 唯一推荐的 LLM 介入方式：假设生成前置
```
非结构化文本（举报信、笔录、舆情）
      ↓ LLM：抽取要素、提出假设雏形
假设雏形（H1~H5 自然语言描述 + 候选数据源）
      ↓ 交给内核：用数据验证
可证伪的假设 + 命中间类 + 交叉等级 + 优先级
```
**LLM 负责"猜"，内核负责"验"。** 猜错无妨——假设自带证伪条件，数据验不过自动降级。

---

## 输出格式（六段，固定）
【庙算基线】【双向盘点】【虚实扫描】【奇正分工】【用间交叉】【全胜校验】

产物落在 `output/`：
- `lineage_clues.json` — 线索流（含假设链 / 溯源行 / 间类 / 优先级 / 处置状态 / 审计链）
- `六段输出.json` — 六段结构
- `q2_overpass_cypher.json` — 图库双轨比对
- `entity_mapping.json` / `review_queue.json` — 实体映射 / 确认队列

---

## 快速命令
```bash
python run_all.py --auto-review --no-cli   # 一键全链路（演示模式）
python run_all.py --operator "李检察官"      # 生产：人工逐条确认
python run_tests.py                         # 5 组测试
python -m scripts.verify_ladybug            # 图库能力验证（5 项）
python -m scripts.q2_overpass_cypher        # Q2 过桥 Cypher + SQL 双轨
```

详细安装与故障排查见 `INSTALL.md`。
