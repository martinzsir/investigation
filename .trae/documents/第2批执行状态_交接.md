# 第 2 批执行状态交接（REQ-002/003/004/005/015/018）

> 更新时间：2026-09-03（WSL 侧复核后更新）。用途：跨机器/跨会话续接。需求出处：`孙武侦查官_实施方案_需求级拆解.md`
> （REQ-002 约 130 行、REQ-003 约 174 行、REQ-004 约 219 行、REQ-005、REQ-015 约 664 行、REQ-018 约 779 行）。

## 一、总体状态

**第 2 批五个需求的代码与测试全部完成。**

- **WSL2 环境（本机，venv `/root/.venvs/inves`）：`run_tests.py` 18 组全绿**（2026-09-03 实测，
  含 mcp 46 项 / e2e 10 项），第 2 批交付在 WSL 侧**已闭环**。
- Windows 原生（另一台机器）：16/18 绿，仅 `mcp`、`e2e` 两组因 **pyarrow 安装残缺**失败
  （已知环境限制，非代码问题；修复步骤见 四/五 节）。
- WSL 复核时发现并修复了一个第 2 批自带缺陷：**test_ac5 全量回归无限递归**（commit `73f33b9`，
  Windows 侧修 pyarrow 前**必须先 git pull**，否则 spec 组会卡 300s 超时，见 四.1）。

## 二、已完成并验证（全部实测通过）

| 需求 | 文件 | 验证 |
|---|---|---|
| REQ-018 影响范围计算 | [core/rebuild_planner.py](../../core/rebuild_planner.py) | `tests.test_rebuild_planner` 8/8 OK（含 AC3 十万边 <1s） |
| REQ-002 语义层读网关 | [core/gateway.py](../../core/gateway.py) | `tests.test_gateway` 6/6 OK |
| REQ-003 直查拦截 | [core/store.py](../../core/store.py) | `tests.test_store_guard` 7/7 OK |
| REQ-003 静态扫描 | [scripts/audit_straight_sql.py](../../scripts/audit_straight_sql.py) | 脚本 `--fail-on-violation` EXIT=0；扫描器反向自证（喂违规代码检出 3 处）也进了测试 |
| REQ-004 增量重建 | [core/ontology.py](../../core/ontology.py) + [scripts/incremental.py](../../scripts/incremental.py) | `tests.test_incremental_semantic` 4/4 OK（AC1 FRESH / AC2 重写 300 行 / AC3 退出码 1 / AC4 幂等跳过 / AC6 命名） |
| REQ-005 隔离 | 同上 incremental.py | `test_quarantine_bad_schema_exit_2` 通过（退出码 2，不装载） |
| REQ-015 L1 特征 | [core/features.py](../../core/features.py) | `tests.test_features` 5/5 OK |
| 第 1 批存量 | test_ontology_version/event_bus/ingest_validate/schemas/rule_schema/reference_integrity/audit_chain | 46 项 OK（1 skip） |

### 本会话关键修复（续接者无需重做）

1. **`scripts/incremental.py`**：watermark 回填三行（`SELECT COUNT(*), MAX(CAST(日期 AS VARCHAR)) FROM {view_name}`）
   从 validate 之后挪到 `if errors:` 隔离块**之后**（约 128–132 行）。坏 schema 分区缺"日期"列时先隔离返回 2，
   不再抛 BinderException。这是 `test_quarantine_bad_schema_exit_2` 由 ERROR 转 OK 的唯一改动。
2. **`scripts/audit_straight_sql.py`** 两处：
   - `cold_scan` 的 SQL 片段取参修正：首参是文件 pattern（文件名含表名正常，不判），SQL 在第 2 位置参 / `extra_where` 关键字参；
   - `scan_file` 对仓库外文件 `relative_to(ROOT)` 抛 ValueError → try/except 回退绝对路径（单测喂临时文件用）。
3. **`tests/test_store_guard.py`** 新增 `test_ac5_static_scanner_detects_violations`：
   喂 6 行样例代码，断言检出 3 处违规（query/execute/cold_scan）且 DDL 放行、语义层 obj_* 放行、read_parquet 字面量放行。
4. **`run_tests.py` GROUPS 从 8 组扩到 18 组**：新增 version/eventbus/ingest/spec/planner/gateway/guard/features/incremental/audit；
   `audit` 组直接跑 `scripts/audit_straight_sql.py --fail-on-violation`。头部 docstring 已同步更新。

## 三、run_tests.py 最新实测结果

**WSL2（本机，2026-09-03，commit 73f33b9）：18 组全绿 ✅**

```
✅ 全部通过：mcp, graph, miaosuan, org, review, disposal, ontology, version,
            eventbus, ingest, spec, planner, gateway, guard, features,
            incremental, audit, e2e
```
（WSL 复核发现的两件事已处理：① 新依赖 `jsonschema` 已装进 venv——清华镜像该包 403，
换阿里云 `pip install jsonschema -i https://mirrors.aliyun.com/pypi/simple/`；
② test_ac5 无限递归已加 `SUNZI_AC5_NESTED` 守卫修复，commit `73f33b9`。）

**Windows 原生 python 3.12.9 miniconda3（另一台机器，pyarrow 修复前）：16/18 绿**

```
16 组绿：graph/miaosuan/org/review/disposal/ontology/version/eventbus/ingest/spec/
         planner/gateway/guard/features/incremental/audit
2 组红：mcp、e2e —— 全部因 ImportError: Missing optional dependency 'pyarrow'
         （e2e 崩在 run_all.py:273 to_parquet；mcp 崩在 ladybug/pyarrow 依赖链）
❌ 失败组：['mcp', 'e2e']
```

## 四、续接后待办（按顺序）

> WSL 侧待办已闭环（18 组全绿）。以下仅为 **Windows 原生机器**的操作清单：

1. **先 `git pull`（拿到 `73f33b9`），再修 pyarrow**——顺序不能反：
   旧版 test_ac5 会 subprocess 嵌套跑全量 run_tests.py 且无递归守卫，
   pyarrow 修好后 spec 组反而会卡 300s 超时；`73f33b9` 已修复。
2. **装 jsonschema**（第 2 批 schemas 校验新依赖）：
   `python -m pip install jsonschema -i https://mirrors.aliyun.com/pypi/simple/`
   （清华镜像该包 403，直连 PyPI 极慢）。
3. **修 pyarrow，跑绿 mcp/e2e**：
   当前 Python 是 `D:\Program Files\miniconda3`，pyarrow 装在用户目录
   `C:\Users\MARTINHOME\AppData\Roaming\Python\Python312\site-packages\pyarrow`，
   但 **pyarrow.libs 目录缺失**（沙箱拦截文件写入导致安装残缺）→
   `pip uninstall pyarrow -y` 后在**非沙箱/管理员终端**重装：
   `python -m pip install pyarrow -i https://pypi.tuna.tsinghua.edu.cn/simple`
   （修好后 `python -c "import pyarrow"` 应打印版本）。
4. `python run_tests.py` **18 组全绿** = Windows 侧收尾完成（WSL 侧已达成）。
5. 收尾杂项（可选）：
   - REQ-004 AC5（受保护 entity_mapping 版本）按计划留给第 4 批 REQ-016，未做；
   - 第 1 批 6 个测试文件已随 spec 组接入 run_tests.py，无遗留。

## 五、当前阻塞详情（中断点）

> 仅限 Windows 原生环境；WSL 侧无此阻塞（pyarrow 完好，21 组全绿）。

- 中断动作：诊断 pyarrow DLL 残缺（`pip show pyarrow` / 查 RECORD）时用户取消。
- 已知事实：`import pyarrow` → `ImportError: DLL load failed while importing lib: 找不到指定的模块`；
  `pyarrow/` 目录有 10 个 DLL 但同级 `pyarrow.libs/` 不存在（正常 wheel 安装必有该目录）；
  安装位置是用户 site-packages（`C:\Users\MARTINHOME\AppData\Roaming\Python\Python312\site-packages`）。
- 结论：**不是版本问题，是安装残缺**。清理重装即可；若在 IDE agent 里装，需先 `pip uninstall`，
  且必须放行沙箱（上次安装 pyarrow.libs 写入被 `TRAE Sandbox Error: hit restricted` 拦截）。

## 六、环境注意事项（Windows 本机）

- PowerShell 5.1：`&&` 非法（用 `;`）、`tail` 不可用（用 `Select-Object -Last N`）；
  profile 执行策略 CLIXML 报错是噪音，测试结果仍可见（OK/FAILED 在尾部）。
- Python：`D:\Program Files\miniconda3`（duckdb/pandas 可用，ladybug 未装 → mcp 组本机本就依赖 pyarrow 先修）。
- 测试是 unittest 不是 pytest：`python -m unittest tests.test_xxx`。
- DuckDB 坑：DDL（CREATE VIEW）不接受 prepared parameter（路径转义内联）；`range()` 返回 UBIGINT，
  日期算术必须 `CAST(... AS INTEGER)`。
- 十万行增量测试套件（test_incremental_semantic）单跑约 3 分钟（主因 compute_version 全表 sha256），功能正确，暂不优化。

## 七、测试-代码对应速查

| 测试文件 | 项数 | 覆盖 |
|---|---|---|
| tests/test_rebuild_planner.py | 8 | AC1 一跳邻域/AC2 批量阈值/AC3 十万行<1s/AC4 环不扩散/AC5 空种子/分区图景 |
| tests/test_gateway.py | 6 | 中文表名/STALE/explain 三键/与直查等价/无自由 SQL/UNBUILT |
| tests/test_store_guard.py | 7 | 拦截/unsafe 三要素/审计落库/max_rows/字面量剔除/cold_scan/AC5 扫描器 |
| tests/test_features.py | 5 | 重启读回/不可变/mark_stale/feature_ref 签名/get_or_compute |
| tests/test_incremental_semantic.py | 4 | AC1 FRESH/AC2 重写300/AC3 缺失退出1/AC4 幂等/AC6 命名 + AC2 隔离退出2 |

## 八、第 3 批执行状态（2026-09-03 完成，P0 权限 REQ-009/010/011）

**全部完成并验证**：run_tests.py **21 组全绿**（WSL，新增 access 7 项/policy 9 项/export 4 项）、
mcp_client_test **51/51**（新增低权限会话场景 5 项）、run_all 冒烟通过。

- **REQ-009 AccessContext**：`core/access.py`（frozen/operator 必填/isolated 拒 LLM）；
  网关/Function/Action 三处签名均接受 access（AC3），access=None → system 旁路全兼容；
  Action 写主体一致性（operator≠access.operator 拒绝）+ human 专属终态。
- **REQ-010 策略**：`ontology/default/policies.json`（11 对象 + 9 链接全显式声明，
  AC5 覆盖率断言）+ `core/policy.py`（fail-closed/角色×clearance 双条件/partial 遮蔽
  310****1234）；接入 gateway.objects/links/count 与 FunctionExecutor.invoke
  （按 inputs 声明检查）。
- **REQ-011 出口接入**：MCP 会话身份经 SUNZI_OPERATOR/ROLE/CLEARANCE/NETWORK 环境变量
  （缺省 system 全旁路）；clue_list 低权限过滤内间线索（实测过滤 1 条）；
  clue_transition 主体一致性 + human 终态门槛；function_invoke 低权限调偏将级函数被拒；
  工具清单无 run_sql（AC4 断言）。export_ladybug 改 argparse CLI（--operator/--role/
  --clearance/--purpose/--destination），对象/链接逐个过策略、越权 export_denied 落审计 +
  stderr 告警 + 退出码 2（AC5）、成功导出 operator/purpose/destination/文件清单落
  audit_chain（AC3）。
- run_tests.py 18→21 组；AGENTS.md 已同步（权限面段落/验证节/权限三纪律）。
- 遗留：REQ-019 `.github/workflows/ci.yml` 仍未建（G 组）；第 4 批（REQ-012~017/020/021-read/024）未开始。

## 九、第 4 批执行状态（2026-09-04 完成，P0 闭环 REQ-012/013/014/016/017 + REQ-020/021-read/024）

**全部完成并验证**：run_tests.py **28 组全绿**（WSL，新增 action 7/writeback 7/reconcile 6/
reviewloop 7/deferred 6/r5knowledge 5/golden 7 共 45 项）、mcp_client_test **62/62**
（工具 9→12，新增 REQ-021 读轨 8 项 + 低权限受限摘要 1 项）、run_all --auto-review 冒烟通过
（entity_mapping person=7/org=2 落表，review 2 条自动 accept，R5 走知识包 py 实现）。

- **REQ-012 Action 两阶段提交**：`core/action_executor.py` action_request 表 +
  submit/approve/dispatch/mark_confirmed/request_status；状态机
  proposed→approved→dispatching→pending_receipt→confirmed（分支 failed/dead_letter）；
  匿名/占位 operator 判定统一 `_is_placeholder_operator()`。
- **REQ-013 Writeback**：`core/outbox.py`（writeback_outbox，幂等键 wb:{action_id}，
  含 last_attempt_at 旧库 ALTER 迁移）+ `core/writeback.py`（WritebackAdapter Protocol、
  StubLedgerAdapter 故障注入 fail/conflict/grant_business_id、WritebackDispatcher；
  外部成功=返回业务号；action_request 表不存在时 information_schema 守卫跳过）。
- **REQ-014 对账重试死信**：`core/reconcile.py` 退避 [60,300,1800,7200,28800]、MAX_ATTEMPTS=5、
  409 不重试转 manual_409、pending_receipt 3600s 幂等重投、差异只报告不覆盖、render_report。
- **REQ-016 review 闭环（含 REQ-004 AC5 回补）**：`core/review_loop.py`（entity_mapping
  受保护表 + review_applied 幂等表；record_accept/record_feedback/apply_accept/apply_decisions）；
  `core/rebuild_planner.py` plan_from_review（重建前查图，种子覆盖合并双方）；
  `core/ontology.py` _load_entity_mapping/_apply_entity_mapping（归并在代理键分配前；
  事件型名字列 raw_*/caller_raw/callee_raw/person_raw 双侧归并保边）；`core/rules.py`
  run_rules 支持 rule_ids 增量过滤；run_all 步骤 5 接线。
- **REQ-017 defer 回捞**：`core/deferred.py` deferred_task 表，唤醒条件 OR 语义
  （on_dataset/after TTL/evidence_count_gte），仅 waiting 可唤醒，recall_stats 回捞率。
- **REQ-024 R5 知识包**：`ontology/default/case_knowledge.json`（knowledge_version 2026.05.01、
  subject_aliases、relation_assertions 含过期断言 valid_until）；`core/functions.py`
  load_case_knowledge + org_interest_links py 注册实现（原硬编码 SQL 已删）；
  `scripts/scan_hardcoded_names.py` --fail-on-hit 中文 LIKE 扫描；人名零硬编码、过期断言排除、
  pack 缺失零命中不报错。
- **REQ-021-read**：MCP 新增 review.list_pending / review.get_evidence / action.status
  三只读工具（tools/list 已声明 inputSchema，readOnlyHint）；正兵及以下 get_evidence
  仅返回 evidence_summary 受限摘要；submit_proposal 写轨推迟第 8 批。
- **REQ-020 Golden Finding 回归**：`tests/fixtures/golden/` 三夹具（baseline 锁
  R1/R2/R4/R6 共 4 findings/7 source_rows；false_positive_trap 同城不同日+非关联法人
  零命中；degraded_single_source 单源降级零命中不崩）+ `tests/test_golden.py`
  （夹具脱敏扫描/状态锁/双跑确定性/drift_report 漂移自检）。配套加固：FunctionExecutor
  对 obj_*/lnk_* 语义表缺失的 CatalogException 返回 degraded 零命中（降级纪律）。
- 事件类型新增 finding.changed / writeback.dead_letter（core/event_bus.py）。
- 遗留：REQ-021 写轨（submit_proposal）第 8 批；REQ-019 CI workflow 仍未建；
  第 4 批改动**尚未 git commit**（待用户确认后提交推送 ontology_v2）。

## 十、第 5 批执行状态（2026-09-05 完成，P1 规则健壮性 REQ-025/027/028/029/030/026）

**全部完成并验证**：run_tests.py **34 组全绿**（新增 overlap 5/threshold 6/derived 5/object_set 5/metrics 5/rule_dsl 6 共 32 项，调整：tests/test_ontology.py test_规则驱动虚实扫描因 REQ-025 抑制改断言）、mcp_client_test **62/62** 保持、run_all --auto-review --no-cli 冒烟通过（线索 9 条 = 1 已立案+8 跟进不变，R2 抑制为预期行为）。

### 各 REQ 改动与验收

#### REQ-025 规则互斥与重叠消解
- **ontology.py RuleSpec** 加 `exclusive_group / primary_rule / overlap_resolution / excludes` 四字段；
- **ontology_loader.py** 装载期：excludes 双向不一致 `warnings.warn`（AC2 单向装载告警）、exclusive_group 内最多一个 `primary_rule=True`；
- **rules.json**：R1（primary）+ R2（secondary）标注 exclusive_group="integer_amount"、overlap_resolution="drop_if_primary_hit"、excludes 双向；
- **core/rules.py**：抽取 `_evaluate_single()`、末段 `_suppress_overlaps()`（组内 primary 命中→次级 findings 移出主列表，写入 suppressed_log：rule_id/suppressed_by_group/rule/reason/级别/候选虚处/source_rows）；每条 finding 挂 `suppressed_log` 副本（AC3 可审计、AC5 抑制原因落盘）；
- AC4：R3/R4/R5/R6 无 exclusive_group 时行为不变；baseline golden 从 4 finding → 3（R1/R4/R6，R2 被抑制），note 中标注为"有意升级"；
- tests/test_rule_overlap.py **5/5 全绿**。

#### REQ-027 阈值策略对象（分位数自适应）
- **ontology/default/thresholds.json**：6 条规则声明（method / bound_param / value / params / fallback / bounded_by / per）。R1/R2/R4/R5/R6 用 absolute 向后兼容；R3 call_frequency_spike 用 relative_median（multiplier=2.0 min_samples=20 fallback=30 bounded 5~1000 per=subject）；
- **core/threshold.py**：`load_thresholds()` / `_collect_samples(R3)` / `resolve_rule_params(store, rule_id, params, seed, force_samples)` ——样本充足走 adaptive，中位数 × multiplier、bounded clamp；样本不足走 fallback 且 is_degraded=True（AC1/2/3）；seed 确定性（AC4）；
- **core/rules.py run_rules()**：在循环前调 resolve_rule_params 写 params，finding 附 `threshold_method / threshold_value / is_degraded`（AC5）；AC6 两批不同分布 fixture 阈值自动变化；
- tests/test_threshold_adaptive.py **6/6 全绿**。

#### REQ-028 DerivedProperty（查询时派生）
- **core/derived.py**：`DerivedProperty` 注册器（register/get/list_all/compute）；cache_policy ∈ {never/ttl/until_source_change/materialized}，结果包装 `{value, computed_at, source_version_set, params_hash, cache}`；
- **AC3 白名单守门**：register 时 Function 必须在 pack 的 functions.json 存在（否则抛 ValueError + "白名单"关键词）；compute 走 FunctionExecutor.invoke；
- **AC5 禁止启发式打分**：`_FORBIDDEN_REGISTRY_NAMES = {"person.risk_score", ...}`，register 前抛错并提示"保持在展示层"；
- AC1 never 策略每次重算（两次 computed_at 可区分）、AC2 until_source_change 版本戳变化失效、AC4 params_hash 变化缓存不命中；
- tests/test_derived.py **5/5 全绿**。

#### REQ-029 ObjectSet 查询构造器
- **core/object_set.py**：AST 节点 Eq/Ne/In/Gt/Lt（值类型断言必须是 primitive 或 list 数值，拒绝 dict/SQL 子句对象），And/Or/Not 只接受 Predicate 子节点（AC5 注入防护）；
- `OntologyObjectSet` frozen 链式 API：`filter() / search_around(link, max_hops=1..10) / with_link_filter()`全部返回新副本，原对象不变（AC1 不可变）；构造全程不访问 store（AC3 惰性，materialize 才触达 gateway.objects + links + seen 去重 DFS AC2 环图不无限步）；
- `explain()` 输出人类可读伪代码（filters、search_around、link_filters，AC4）；
- tests/test_object_set.py **5/5 全绿**。And/Or 改为非 frozen 普通类（支持 *args 展开）。

#### REQ-030 规则运行时度量
- **core/metrics.py**：`ensure_rule_run_metric()` 幂等建表 rule_run_metric（PK=(run_id, rule_id, rule_version) 保证 AC4 分层）；`rule_version()` = sha1(rule_id+rule_text+params)[:16]；`record_run()` 每规则一行（INSERT OR IGNORE 幂等 AC1）；`verdict_backfill(verdict ∈ {verified, excluded}, override, rule_version=?)`（AC2 回流 later_verified/later_excluded/override_count）；三率 `hit_rate / precision_estimate / override_rate`（分母 0→None，AC3）；
- **AC5 权限遮蔽**：正兵及以下查 R3/R4 敏感规则——evaluated/hit 舍入 10 进制量级、后验列置空、附 `_access_note`；非敏感规则全显；system/偏将及以上全量；
- tests/test_metrics.py **5/5 全绿**。

#### REQ-026 规则 DSL（组合与时序）
- **core/rule_dsl.py**：AST parse({"all"/"any"/"not"/{"rule":"RID","within_days":N}}) → DslNode（RuleRef/All/Any/Not）；校验：rule_ref 存在、within_days 正整数、depth ≤ MAX_DEPTH=5（防表达式炸弹 AC5，超深抛 ValueError 含"深度"与阈值）；
- `compile()` → CallPlan（leaf_ids + explain 文本）；`evaluate()` 先 run_rules 只跑叶规则（最小开销），再 `_cross_check_within_days` 对 all 下 RuleRef.within_days 做日期窗口；**任何 finding 缺日期列一律保守判为窗口不通过**（绝不把缺口当通过，返回 degraded_note 含"日期"关键词）；
- explain() 形如 `AND(WITHIN(R4, 1d), R6, NOT(R4))` 可读计划 AC6；
- tests/test_rule_dsl.py **6/6 全绿**。

### 文件变更总览

| 类型 | 数量 | 明细 |
|---|---|---|
| 新核心模块 | 5 | core/threshold.py、core/derived.py、core/object_set.py、core/metrics.py、core/rule_dsl.py |
| 新声明文件 | 1 | ontology/default/thresholds.json |
| 新测试 | 6 | tests/test_rule_overlap/threshold_adaptive/derived/object_set/metrics/rule_dsl.py |
| 改核心 | 3 | core/ontology.py（RuleSpec 字段）、core/ontology_loader.py（装载校验）、core/rules.py（抑制+阈值元数据） |
| 改声明 | 1 | ontology/default/rules.json（R1/R2 互斥组标注） |
| 改测试注册 | 2 | run_tests.py 加 6 组；test_ontology.py 虚实扫描断言按 REQ-025 调整 |
| 改 golden | 1 | tests/fixtures/golden/baseline.json（4→3 finding，已在 note 标注） |

### 遗留（未在本批范围）
- REQ-019 `.github/workflows/ci.yml`：仍未建（P1 G 组跨批外溢，建议后续单批）。
- REQ-021-write submit_proposal：第 8 批。
- **尚未 git commit**（用户明确要求前不提交、不推送 ontology_v2）。

## 十一、第 6 批执行状态（2026-09-05 完成，LLM 治理基座 REQ-038/033/039/031/032）

**全部完成并验证**：run_tests.py **39 组全绿**（新增 llmpolicy 6/proposal 10/injection 5/caselib 5/params 6 共 32 项）、mcp_client_test **62/62** 保持（MCP 未改）、run_all --auto-review --no-cli 冒烟通过（线索 9 条不变，新表全部 CREATE IF NOT EXISTS）。批内强制顺序 038→033→039 已遵守。全程纯离线：LLM 调用闸门默认 isolated 拒网，fake_invoke 测试替身。

### 各 REQ 改动与验收

#### REQ-038 llm_policy 与脱敏闸门
- **ontology/default/llm_policy.json**：LLM 使用策略声明（network / allowed_models / pii_rules），装载 fail-closed（文件缺/非法/缺字段 → 回落内置 isolated+空模型白名单，`_source` 标记来源）；
- **core/llm/redact.py**：`redact_text()` 三型 PII 正则（身份证/手机/银行卡**均带数字边界**防银行卡内子串误匹配），遮蔽风格前3后4星号；`tokenize_name()`→`当事人#<sha1[:6]>`（同名同 token）；`redact_payload()` 递归遮蔽（轨迹/地点→precise_track、通话内容/备注/description→call_content、人名类键→tokenize）；`build_redacted_context(findings)` 每项只出 rule_id/级别/dimension/聚合计数/evidence_uris/is_degraded；`log_llm_call()` 建 llm_call_log 表 + AuditChain；`call_llm()` 闸门序列：require_llm_allowed→模型白名单→redaction_hash 存在+**复扫排除 redaction_hash/pii_rescan 字段后 PII 零命中**（64 位 hash 偶现手机号形态）→fake_invoke 未注入即拒（fallback=deterministic_only）→无论放行/拒绝都落日志，拒抛 LLMBlockedError；
- tests/test_llm_policy.py **6/6 全绿**。

#### REQ-033 ProposalStore 强类型提案
- **schemas/proposal.schema.json**：Draft-07 提案 schema；
- **core/proposal.py**：`validate_proposal()` 七校验：①schema ②candidate.function ∈ pack functions ③params 逐参数复用 `core.functions.check_param_value`（类型/enum 白名单）④evidence_row_uris 正则 `^(obj|lnk)_...`，**obj 主键列以 ontology ObjectType.pk 为准（transaction→txn_id）**，lnk 查声明端点列 ⑤rule_draft 递归禁 writeback/action/auto_approve/execute 等写回键 ⑥explanation 禁 status/transition 等状态键 + 指令值正则（"置为已立案/跳过复核"）⑦confidence 只允许在 `._sort_hint` 路径下（`confidence_only_sorts()` 辅助断言）；
- `ProposalStore`：proposal 表（PK `pp-`）、submit（author 非匿名）、get/list、decide（draft→approved/rejected，过期不可批，重复 decide 拒，落 AuditChain，**永不自动生效**）；
- 踩坑修复：`_iter_keys()` 必须下发 list 内标量（原实现 list 字符串元素漏检）；path 父节点判定用 `path.rsplit(".",1)[0].endswith("._sort_hint")`；
- tests/test_proposal.py **10/10 全绿**。

#### REQ-039 提示注入防护
- **core/llm/guard.py**：`INJECTION_PATTERNS` 7 条（ignore_previous/override_verdict/skip_review/sql_injection/role_play=high；stealth/reveal_prompt=med）；`scan_text()` 返回命中区间；`wrap_untrusted(text,source)` 打框 + sha256；`scan_bundle(frames)` high 整帧隔离 quarantine；`sanitize_candidate(raw,kind)` 白名单字段（rule_draft/parameter_draft/alignment_review/explanation 各有 `_CANDIDATE_FIELDS`，`_sort_hint` 通用透传）；`assert_no_status_change()` 复用 proposal 的禁键/指令正则；`raw_evidence_fragment()` 取原始行，**非 system 上下文过 PolicyEngine**（见习读 transaction/trackpoint/tipoff 被拒）；
- **tests/fixtures/injection/** 4 份攻击样本 .txt；
- tests/test_injection.py **5/5 全绿**。

#### REQ-031 案例库片段
- **core/case_library.py**：case_fragment 表（fragment_id `cf-<hex12>`，rule_id/rule_version/case_id/ontology_version/pattern/evidence/outcome∈verified|excluded/confidence/legal_basis/redaction_hash/clue_id/audit_event_id）；
- `settle_fragment()` 四质量门一次性收集错误抛 CaseLibraryError：①**终态门**查 clue_disposal_status（已固证→verified/已排除→excluded，待查/查证中拒，outcome 必须与状态一致）②适用条件门（pattern 非空、≥15 字、**必须包含 rule_id 字符串**）③legal_basis 非空 ④**脱敏门**（scan_pii 零命中 + subject_aliases 真名集比对，含真名即拒；允许 `当事人#xxx` token）；过闸后 rule_version=`core.metrics.rule_version()`、ontology_version=`current_version()`（异常→"unknown"），落 AuditChain；`search(rule_id, keyword, outcome, case_id)`；
- tests/test_case_library.py **5/5 全绿**（4 条预置状态线索：已固证/已排除/待查/查证中）。

#### REQ-032 参数治理
- **core/parameters.py**：parameter_set 表（set_id `ps-<scope>-v<n>`、scope=rule_id、version_seq、values、provenance、status draft/shadow/production/retired）+ parameter_proposal 表（`pp-`、evidence/risk/rollback_version/sample_size/status）；
- `draft_set()` 恒新 version_seq **AC1 不覆盖**；`effective_values()` **AC2 只读 production 行**（pending/shadow 不生效）；`propose()` **AC6 sample_size 必传且 ≥20 否则拒入审批**（与阈值策略 min_samples 对齐，fail-closed）；`approve(mode=shadow|production)`（production 时同 scope 旧 production 自动 retired，落 AuditChain）；`shadow_compare(old,new,store,rule_id)` **AC3**：两组 values 各合并 rule.params 跑一次白名单 FunctionExecutor.invoke，行签名集合比对 added/removed/stable（R1 round_unit 10000→300000：1 行→0 行实测）；`rollback(scope, version)` **AC4** 当前 production→retired、目标→production，函数结果随之复原；provenance（metrics_run_id/sample_size/basis）随版本落盘 **AC5**；
- tests/test_parameters.py **6/6 全绿**。

### 文件变更总览

| 类型 | 数量 | 明细 |
|---|---|---|
| 新核心模块 | 6 | core/llm/__init__.py、core/llm/redact.py、core/llm/guard.py、core/proposal.py、core/case_library.py、core/parameters.py |
| 新声明/schema | 2 | ontology/default/llm_policy.json、schemas/proposal.schema.json |
| 新测试夹具 | 1 目录 | tests/fixtures/injection/（4 份 .txt 攻击样本） |
| 新测试 | 5 | tests/test_llm_policy.py、test_proposal.py、test_injection.py、test_case_library.py、test_parameters.py |
| 改测试注册 | 1 | run_tests.py 加 5 组（llmpolicy/proposal/injection/caselib/params），34→39 组 |

### 设计要点与遗留
- 三批 LLM 基座均为**纯库代码**：检测器/rules/MCP 接线未改，LLM 无网络默认拒绝，所有写路径仍只有 ActionExecutor；proposal 与 parameter_proposal 审批后**不自动生效**（proposal 永不自动生效；参数上线需人工另行接线，run_rules 仍读 rules.json）。
- REQ-021-write submit_proposal 的 **MCP 工具接线仍未做**（ProposalStore 核心已就绪，MCP 暴露留后续批次）；案例库/参数治理的 MCP 工具同样未暴露。
- 脱敏/注入/案例库均未接入 run_all 主链路（库能力 + 单测覆盖，接线属后续批次）。
- REQ-019 `.github/workflows/ci.yml`：仍未建。
- **第 4/5/6 批改动均未 git commit**（用户明确要求前不提交、不推送 ontology_v2）；游离文件 data/t3.sqlite 勿提交。

