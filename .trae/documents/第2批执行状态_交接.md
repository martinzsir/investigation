# 第 2 批执行状态交接（REQ-002/003/004/005/015/018）

> 更新时间：2026-09-03（WSL 侧复核后更新）。用途：跨机器/跨会话续接。需求出处：`孙武侦查官_实施方案_需求级拆解.md`
> （REQ-002 约 130 行、REQ-003 约 174 行、REQ-004 约 219 行、REQ-005、REQ-015 约 664 行、REQ-018 约 779 行）。

## 一、总体状态

**第 2 批五个需求的代码与测试全部完成。**

- **WSL2 环境（本机，venv `/root/.venvs/inves`）：`run_tests.py` 18 组全绿**（2026-09-03 实测，
  含 mcp 46 项 / e2e 10 项），第 2 批交付在 WSL 侧**已闭环**。
- Windows 原生（另一台机器）：16/18 绿，仅 `mcp`、`e2e` 两组因 **pyarrow 安装残缺**失败
  （已知环境限制，非代码问题；修复步骤见 四/五 节）。
- WSL 复核时发现并修复了一个第 2 批自带缺陷：**test_ac5 全量回归无限递归**（commit `73f33b9`，
  Windows 侧修 pyarrow 前**必须先 git pull**，否则 spec 组会卡 300s 超时，见 四.1）。

## 二、已完成并验证（全部实测通过）

| 需求 | 文件 | 验证 |
|---|---|---|
| REQ-018 影响范围计算 | [core/rebuild_planner.py](../../core/rebuild_planner.py) | `tests.test_rebuild_planner` 8/8 OK（含 AC3 十万边 <1s） |
| REQ-002 语义层读网关 | [core/gateway.py](../../core/gateway.py) | `tests.test_gateway` 6/6 OK |
| REQ-003 直查拦截 | [core/store.py](../../core/store.py) | `tests.test_store_guard` 7/7 OK |
| REQ-003 静态扫描 | [scripts/audit_straight_sql.py](../../scripts/audit_straight_sql.py) | 脚本 `--fail-on-violation` EXIT=0；扫描器反向自证（喂违规代码检出 3 处）也进了测试 |
| REQ-004 增量重建 | [core/ontology.py](../../core/ontology.py) + [scripts/incremental.py](../../scripts/incremental.py) | `tests.test_incremental_semantic` 4/4 OK（AC1 FRESH / AC2 重写 300 行 / AC3 退出码 1 / AC4 幂等跳过 / AC6 命名） |
| REQ-005 隔离 | 同上 incremental.py | `test_quarantine_bad_schema_exit_2` 通过（退出码 2，不装载） |
| REQ-015 L1 特征 | [core/features.py](../../core/features.py) | `tests.test_features` 5/5 OK |
| 第 1 批存量 | test_ontology_version/event_bus/ingest_validate/schemas/rule_schema/reference_integrity/audit_chain | 46 项 OK（1 skip） |

### 本会话关键修复（续接者无需重做）

1. **`scripts/incremental.py`**：watermark 回填三行（`SELECT COUNT(*), MAX(CAST(日期 AS VARCHAR)) FROM {view_name}`）
   从 validate 之后挪到 `if errors:` 隔离块**之后**（约 128–132 行）。坏 schema 分区缺"日期"列时先隔离返回 2，
   不再抛 BinderException。这是 `test_quarantine_bad_schema_exit_2` 由 ERROR 转 OK 的唯一改动。
2. **`scripts/audit_straight_sql.py`** 两处：
   - `cold_scan` 的 SQL 片段取参修正：首参是文件 pattern（文件名含表名正常，不判），SQL 在第 2 位置参 / `extra_where` 关键字参；
   - `scan_file` 对仓库外文件 `relative_to(ROOT)` 抛 ValueError → try/except 回退绝对路径（单测喂临时文件用）。
3. **`tests/test_store_guard.py`** 新增 `test_ac5_static_scanner_detects_violations`：
   喂 6 行样例代码，断言检出 3 处违规（query/execute/cold_scan）且 DDL 放行、语义层 obj_* 放行、read_parquet 字面量放行。
4. **`run_tests.py` GROUPS 从 8 组扩到 18 组**：新增 version/eventbus/ingest/spec/planner/gateway/guard/features/incremental/audit；
   `audit` 组直接跑 `scripts/audit_straight_sql.py --fail-on-violation`。头部 docstring 已同步更新。

## 三、run_tests.py 最新实测结果

**WSL2（本机，2026-09-03，commit 73f33b9）：18 组全绿 ✅**

```
✅ 全部通过：mcp, graph, miaosuan, org, review, disposal, ontology, version,
            eventbus, ingest, spec, planner, gateway, guard, features,
            incremental, audit, e2e
```
（WSL 复核发现的两件事已处理：① 新依赖 `jsonschema` 已装进 venv——清华镜像该包 403，
换阿里云 `pip install jsonschema -i https://mirrors.aliyun.com/pypi/simple/`；
② test_ac5 无限递归已加 `SUNZI_AC5_NESTED` 守卫修复，commit `73f33b9`。）

**Windows 原生 python 3.12.9 miniconda3（另一台机器，pyarrow 修复前）：16/18 绿**

```
16 组绿：graph/miaosuan/org/review/disposal/ontology/version/eventbus/ingest/spec/
         planner/gateway/guard/features/incremental/audit
2 组红：mcp、e2e —— 全部因 ImportError: Missing optional dependency 'pyarrow'
         （e2e 崩在 run_all.py:273 to_parquet；mcp 崩在 ladybug/pyarrow 依赖链）
❌ 失败组：['mcp', 'e2e']
```

## 四、续接后待办（按顺序）

> WSL 侧待办已闭环（18 组全绿）。以下仅为 **Windows 原生机器**的操作清单：

1. **先 `git pull`（拿到 `73f33b9`），再修 pyarrow**——顺序不能反：
   旧版 test_ac5 会 subprocess 嵌套跑全量 run_tests.py 且无递归守卫，
   pyarrow 修好后 spec 组反而会卡 300s 超时；`73f33b9` 已修复。
2. **装 jsonschema**（第 2 批 schemas 校验新依赖）：
   `python -m pip install jsonschema -i https://mirrors.aliyun.com/pypi/simple/`
   （清华镜像该包 403，直连 PyPI 极慢）。
3. **修 pyarrow，跑绿 mcp/e2e**：
   当前 Python 是 `D:\Program Files\miniconda3`，pyarrow 装在用户目录
   `C:\Users\MARTINHOME\AppData\Roaming\Python\Python312\site-packages\pyarrow`，
   但 **pyarrow.libs 目录缺失**（沙箱拦截文件写入导致安装残缺）→
   `pip uninstall pyarrow -y` 后在**非沙箱/管理员终端**重装：
   `python -m pip install pyarrow -i https://pypi.tuna.tsinghua.edu.cn/simple`
   （修好后 `python -c "import pyarrow"` 应打印版本）。
4. `python run_tests.py` **18 组全绿** = Windows 侧收尾完成（WSL 侧已达成）。
5. 收尾杂项（可选）：
   - REQ-004 AC5（受保护 entity_mapping 版本）按计划留给第 4 批 REQ-016，未做；
   - 第 1 批 6 个测试文件已随 spec 组接入 run_tests.py，无遗留。

## 五、当前阻塞详情（中断点）

> 仅限 Windows 原生环境；WSL 侧无此阻塞（pyarrow 完好，21 组全绿）。

- 中断动作：诊断 pyarrow DLL 残缺（`pip show pyarrow` / 查 RECORD）时用户取消。
- 已知事实：`import pyarrow` → `ImportError: DLL load failed while importing lib: 找不到指定的模块`；
  `pyarrow/` 目录有 10 个 DLL 但同级 `pyarrow.libs/` 不存在（正常 wheel 安装必有该目录）；
  安装位置是用户 site-packages（`C:\Users\MARTINHOME\AppData\Roaming\Python\Python312\site-packages`）。
- 结论：**不是版本问题，是安装残缺**。清理重装即可；若在 IDE agent 里装，需先 `pip uninstall`，
  且必须放行沙箱（上次安装 pyarrow.libs 写入被 `TRAE Sandbox Error: hit restricted` 拦截）。

## 六、环境注意事项（Windows 本机）

- PowerShell 5.1：`&&` 非法（用 `;`）、`tail` 不可用（用 `Select-Object -Last N`）；
  profile 执行策略 CLIXML 报错是噪音，测试结果仍可见（OK/FAILED 在尾部）。
- Python：`D:\Program Files\miniconda3`（duckdb/pandas 可用，ladybug 未装 → mcp 组本机本就依赖 pyarrow 先修）。
- 测试是 unittest 不是 pytest：`python -m unittest tests.test_xxx`。
- DuckDB 坑：DDL（CREATE VIEW）不接受 prepared parameter（路径转义内联）；`range()` 返回 UBIGINT，
  日期算术必须 `CAST(... AS INTEGER)`。
- 十万行增量测试套件（test_incremental_semantic）单跑约 3 分钟（主因 compute_version 全表 sha256），功能正确，暂不优化。

## 七、测试-代码对应速查

| 测试文件 | 项数 | 覆盖 |
|---|---|---|
| tests/test_rebuild_planner.py | 8 | AC1 一跳邻域/AC2 批量阈值/AC3 十万行<1s/AC4 环不扩散/AC5 空种子/分区图景 |
| tests/test_gateway.py | 6 | 中文表名/STALE/explain 三键/与直查等价/无自由 SQL/UNBUILT |
| tests/test_store_guard.py | 7 | 拦截/unsafe 三要素/审计落库/max_rows/字面量剔除/cold_scan/AC5 扫描器 |
| tests/test_features.py | 5 | 重启读回/不可变/mark_stale/feature_ref 签名/get_or_compute |
| tests/test_incremental_semantic.py | 4 | AC1 FRESH/AC2 重写300/AC3 缺失退出1/AC4 幂等/AC6 命名 + AC2 隔离退出2 |

## 八、第 3 批执行状态（2026-09-03 完成，P0 权限 REQ-009/010/011）

**全部完成并验证**：run_tests.py **21 组全绿**（WSL，新增 access 7 项/policy 9 项/export 4 项）、
mcp_client_test **51/51**（新增低权限会话场景 5 项）、run_all 冒烟通过。

- **REQ-009 AccessContext**：`core/access.py`（frozen/operator 必填/isolated 拒 LLM）；
  网关/Function/Action 三处签名均接受 access（AC3），access=None → system 旁路全兼容；
  Action 写主体一致性（operator≠access.operator 拒绝）+ human 专属终态。
- **REQ-010 策略**：`ontology/default/policies.json`（11 对象 + 9 链接全显式声明，
  AC5 覆盖率断言）+ `core/policy.py`（fail-closed/角色×clearance 双条件/partial 遮蔽
  310****1234）；接入 gateway.objects/links/count 与 FunctionExecutor.invoke
  （按 inputs 声明检查）。
- **REQ-011 出口接入**：MCP 会话身份经 SUNZI_OPERATOR/ROLE/CLEARANCE/NETWORK 环境变量
  （缺省 system 全旁路）；clue_list 低权限过滤内间线索（实测过滤 1 条）；
  clue_transition 主体一致性 + human 终态门槛；function_invoke 低权限调偏将级函数被拒；
  工具清单无 run_sql（AC4 断言）。export_ladybug 改 argparse CLI（--operator/--role/
  --clearance/--purpose/--destination），对象/链接逐个过策略、越权 export_denied 落审计 +
  stderr 告警 + 退出码 2（AC5）、成功导出 operator/purpose/destination/文件清单落
  audit_chain（AC3）。
- run_tests.py 18→21 组；AGENTS.md 已同步（权限面段落/验证节/权限三纪律）。
- 遗留：REQ-019 `.github/workflows/ci.yml` 仍未建（G 组）；第 4 批（REQ-012~017/020/021-read/024）未开始。
