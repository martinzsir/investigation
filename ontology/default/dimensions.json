{
  "schema_version": 2,
  "_note": "REQ-G-011 侦查维度声明（庙算五维覆盖度模型）。DIMENSIONS 不再硬编码于 core/hypotheses.py，改由本文件声明。**code 为机器标识符（rules.json / hypothesis_patterns.json 一律引用 code），name 仅作展示名（UI/报告）**——此前用中文 name 当标识符，换领域时维度名变更会同时打断引用链与展示，且无稳定主键。装载期交叉校验：引用必须是已声明的 code；未声明硬失败。新增维度不改 Python 即可被规则引用并参与覆盖度。source_object_types 为该维度证据来源的语义对象（元数据，供溯源）。",
  "_compat": "code 缺失的旧维度声明，装载期自动取 name 作为 code（fallback），保证旧案件包零行为变化；新声明必须显式给 code。",
  "dimensions": [
    {"code": "fund", "name": "资金", "note": "银行流水/资金往来异常", "source_object_types": ["transaction"]},
    {"code": "comm", "name": "通讯", "note": "通话/联系频次与对象", "source_object_types": ["call"]},
    {"code": "behavior", "name": "行为", "note": "轨迹同框/行为模式/空间聚集", "source_object_types": ["trackpoint", "location"]},
    {"code": "relation", "name": "关系", "note": "工商/社会关系网络", "source_object_types": ["person", "org"]},
    {"code": "time", "name": "时间", "note": "时间耦合/先后顺序（跨对象属性）", "source_object_types": ["transaction", "call", "trackpoint"]}
  ]
}
